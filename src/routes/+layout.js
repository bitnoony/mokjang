import { supabase } from "$lib/supabaseClient";

export async function load() {
	// const {
	// 	data: { user },
	// } = await supabase.auth.getUser();
	// console.log(user);

	// 최상위 레이아웃 js 인데,
	// getUser 하고 유저 정보가 있으면 pass
	// 없으면 login
}
